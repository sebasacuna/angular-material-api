server:
  port: ${port}
  compression:
    enabled: true
  http2:
    enabled: true
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: ${username}
    password: ${password}
    url: ${url}

